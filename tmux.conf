# pefix key is "CTRL-j"
set -g prefix C-j

set -g display-panes-time 10000
set-window-option -g mode-keys vi

# share clipboard (with vim like key bind)
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi C-v send-keys -X rectangle-toggle
if-shell "uname | grep -q Linux" {
  bind-key -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "wl-copy"
  bind-key -T copy-mode-vi Enter send-keys -X copy-pipe-and-cancel "wl-copy"
} {
  bind-key -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "pbcopy"
  bind-key -T copy-mode-vi Enter send-keys -X copy-pipe-and-cancel "pbcopy"
}

# open window and pane inheriting from the current directory
bind-key c new-window -c '#{pane_current_path}'
bind-key % split-window -v -c '#{pane_current_path}'
bind-key '"' split-window -h -c '#{pane_current_path}'

# resize pane with vim like key bind
bind-key -r H resize-pane -L 5
bind-key -r J resize-pane -D 5
bind-key -r K resize-pane -U 5
bind-key -r L resize-pane -R 5

# status bar (Tokyo Night Storm: dark base + accent chips, matches the zsh prompt)
set-option -g status-interval 5
set-option -g status-justify left
set-option -g status-style "bg=#24283b,fg=#c0caf5"

# left: no session name (single "main" session always) — start at the window list
set-option -g status-left ''
set-option -g status-left-length 0

# windows: current = purple chip (pairs with the prompt's purple git branch),
#          others = muted gray. #F shows flags (* current, - last, Z zoomed).
# Colors live in the *-style options (not the format) so the list fill stays
# on status-style and the current bg does not bleed across the bar.
set-option -g window-status-separator ''
set-option -g window-status-style 'fg=#565f89,bg=#24283b'
set-option -g window-status-current-style 'fg=#1f2335,bg=#bb9af7,bold'
set-option -g window-status-format ' #I #W#F '
set-option -g window-status-current-format ' #I #W#F '

# right:  path  │  host  │  date time   (~ substitution via the shell)
set-option -g status-right-length 80
set-option -g status-right '#[fg=#7dcfff]  #(echo "#{pane_current_path}" | sed "s|$HOME|~|") #[fg=#414868]│#[fg=#9ece6a]  #H #[fg=#414868]│#[fg=#e0af68]  %Y-%m-%d #[fg=#7aa2f7]%H:%M '

# command line / copy-mode selection colors
set-option -g message-style "bg=#bb9af7,fg=#1f2335"
set-option -g mode-style    "bg=#3d59a1,fg=#c0caf5"

# true color
set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",xterm-256color:RGB"

# focus-events
set-option -g focus-events on
